# Tiny FastAPI queue proxy in front of FastSD.
FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir "fastapi==0.115.*" "uvicorn[standard]==0.32.*" "httpx==0.27.*"
COPY queue_app.py .
EXPOSE 9000
CMD ["uvicorn", "queue_app:app", "--host", "0.0.0.0", "--port", "9000"]

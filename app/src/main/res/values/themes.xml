<resources>
    <!-- Base theme applied to the window before Compose takes over. -->
    <style name="Theme.RpgNpcGenerator" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
